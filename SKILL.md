---
name: jerryslide
description: >
  做在线会议共享屏幕用的 HTML slide —— 自包含单文件、零外链、离线可用、可导 PDF。
  中英混排、学术简约风（白底 + 红色）。含 12 页零件库模板、构建脚本、机械验收。
  不适用于：要 .pptx / Google Slides 的；纯英文受众的；投影仪现场演讲的（字号推导完全不同）；
  需要逐元素动画的（这套是「一次按键一页」）。
disable-model-invocation: true
---

# JerrySlide · 屏幕共享用的 slide

**命名约定**：配色统一称“红色”，不附带公司或品牌名称；模板、示例的封面和页脚不预置公司名称。

**这套规范的前提很窄**：屏幕共享 · 对方窗口 ≥800px · 中英混排 · 红色。
**前提之外它不是中性的，它会主动害人** —— 所以入口是显式的，你不会被自动匹配到它。

> **你是被 `/skill:jerryslide` 显式叫起来的。** 先确认下面「不适用」那节没命中，
> 再往下走。

---

> **路径约定**：本 skill 的目录记为 `$SKILL` —— 就是 `SKILL.md` 所在的那个目录，
> π 加载 skill 时会给出。本文所有 `scripts/…`、`assets/…`、`references/…`
> **都相对于 `$SKILL`**，调用时写成完整路径 `"$SKILL/scripts/…"`。
> **命令在你的 deck 工作目录里执行** —— deck 是交付物，不要写进 skill 目录。
> 这套 skill 不依赖 `$SKILL` 以外的任何自有文件。

## 0 · 第一步永远是环境体检

```bash
"$SKILL/scripts/build.sh" --doctor          # 只读，不碰任何文件
```

**不要跳过。** 缺环境的话，它原来会崩在第三步（导出 PDF）—— 而那时
**起手 + 写页面已经把几十分钟的工作量花掉了**。缺什么它会告诉你怎么补。

---

## 1 · 什么时候**不**用

| 场景 | 为什么不行 |
|---|---|
| 要 `.pptx` / Google Slides | 交付形态就是**单文件 HTML + PDF**，不做 pptx |
| 投影仪现场演讲 | `u` 由场地物理尺寸决定，不由窗口宽度决定 —— **整套推导失效** |
| 纯英文受众 | 字体策略的前提是「中英混排要齐平」 |
| 需要逐元素动画 | 这套是**一次按键一页**，没有动画层 |
| 要用现成的公司 deck 模板 | 它是另一套令牌，混进来会两个体系打架 |

---

## 2 · 工作流：五条命令

```bash
# ① 起手（第二个参数是 deck 的 <title>，不给就用文件名）
"$SKILL/scripts/newdeck.py" 我的deck.html "我的 deck 全名"

# ② 把页面抽出来改 —— 抽出来是 ~10 KB，原文件 450 KB
"$SKILL/scripts/setpages.py" 我的deck.html --extract
#     编辑 我的deck.pages.html

# ③ 贴回去（检查全过才落盘）
"$SKILL/scripts/setpages.py" 我的deck.html

# ③.5 按墨迹居中：尾页 Q & A 与页脚 deck 名（量测 → 写进 :root → 复测）
"$SKILL/scripts/inkcenter.py" 我的deck.html

# ④ 字体 → PDF → 验收，一条命令
"$SKILL/scripts/build.sh" 我的deck.html
```

**第 ③.5 步为什么存在**：CSS 居中的对象是**字宽盒子**，人眼看的是**墨迹**，两者差在
字形左右边距 —— 短字符串尤其明显，`Q & A` 与页脚那句差 4px，表现是「两段空隙不等宽」
（空隙短时占 6%，看得出来）。值由脚本量测写入，**不要手改**；文案一改就重跑它。
推导与实测数据在 `references/RULES.md` §4「居中对象是墨迹」。

**为什么页面要抽出来改**：一份 deck 450 KB，其中 400 KB 是内联字体的 base64。
在里面找锚点很难受，**而且锚点找错是静默吞内容的**。
`setpages.py` 保证**边界以外逐字节不动**（抽出来再贴回去 `md5` 相同）。

**`<title>` 也归它管**（它住在外壳里，但它是「这份 deck 的内容」）：
`"$SKILL/scripts/setpages.py" deck.html --title "新的全名"`

---

## 3 · 铁律

**每条都有一句 why。不知道 why 就会「出于好意」违反它。**

1. **不用 1px 线。** 屏幕共享画面被视频编码压过 —— 实测 1px `#d0d0d0` 线
   **只剩 4% 对比度**，等于没有。**用底色块代替线：面比线抗压 20 倍。**

2. **不用 `box-shadow` / `linear-gradient`。** 导出 PDF 时浏览器会生成
   **透明度软掩码**。屏幕上完全看不出，某些阅读器和打印机上会露出灰底。

3. **字体必须切片后内联。** 否则：① 对方没装 → 换行全变 → 页面溢出
   ② 可变字体在 PDF 里没有表示 → 碎成一个个小图形（Type 3）→ 大 6 倍、不能搜。
   **跑 `build.sh` 就自动做了这件事。**

4. **改完任何文字，必须重跑字体子集化。** 漏跑的后果是**静默的** ——
   新字不在子集里，掉回系统字体，**屏幕上完全看不出来**。
   **用 `build.sh` 就不会忘**（它把这一步包进去了）。

5. **放不下就删字，不许缩字号。** 字号是由「对方窗口有多宽」推出来的，
   不是审美选择。**缩字号 = 拆掉整个体系的前提。** 这是本 skill 里最反本能的一条。

6. **一份 deck 只用一个底色**（全白 或 全深）。**不许逐页切深浅。**
   单页看永远看不出问题，连着翻才发现「像两套 deck 拼的」。

7. **面的色偏 ≥ 20 或灰阶差 ≥ 6，否则会被压成一色。** 最浅能用的面是 `#f2f2f2`。
   **禁用 `#fcfcfd` 那一档**（压缩后和 `#fafafa` / `#f6f6f6` 全塌成同一个灰）。

8. **专有名词保留英文原名**（Agent Loop · context · checkpoint）。
   自己造的比喻用中文。**不要因为怕术语就自造中文译名 —— 等于让观众学两遍。**

9. **标题是可证伪的断言，不是名词短语。** 「细线会消失」✓ /「线的可见性」✗

10. **页数由时间定，不由内容定**（10 分钟 ≈ 8 页）。

11. **不用填充语、夸张词、黑话。** 「值得注意的是」「综上所述」「颠覆性」「赋能」
    「闭环」—— **不是难听，是占位置**：一页就那么点字，它们占掉的位置本可以是证据。
    **投影时观众只有一次机会**（报告读者能回读，观众不能）。
    `check.py` 机械查；引用别人的原话用「」括起来即豁免。
    文风全表（结论先行 / 用证据代替形容 / 同一概念一种写法）见 `references/RULES.md` §7。
    **注解（`.caveat`）不是填空白的料** —— 内容少就让它在正文区居中，
    不是加一句总结把 check 的警告垫平（那样数字绿了、页面变差，见 FAILURES F16）。

---

## 4 · 速查

### 令牌

```
--u: 28px        ← 所有尺寸都是它的倍数。u = 22400 / Sv，Sv=800px
--fs-cover 84   --fs-h1 58   --fs-quote 40
--fs-lead 34    --fs-key 34  --fs-body 28  ← 正文下限
--fs-note 23    --fs-kicker 19   --fs-foot 16

--paper #ffffff   --ink #101010   --ink2 #454545
--muted #767676   ← 最浅合格的灰（4.54:1）
--accent #a70e13  ← 红色
--face #f2f2f2  --face-2 #eaeaea  --face-3 #e4e4e4   ← 面的阶梯
--line: 0px solid #b8b8b8        ← 已决定：全部不用线
```

### deck 级结构（三件套，`check.py` 逐条判）

```
第 1 页 = 封面 (.cover)      必须
第 2 页 = 目录 (.toc)        必须；条目 ≤5 行，超了按主题归组
末  页 = 收尾 (.end + .qa)   必须；标题 Thanks ＋ 正中 Q & A
```

### 页面骨架（五个锚点，只有 ③ 伸缩）

```
① .ttl      标题行  .bar（红竖块）+ h1（红色，全页唯一标题）
                          ← 标题必须一行；折行 check.py 判失败（封面例外）
② .lead     导语（可选）      ← 写了就和落点句抢地方
③ .body     正文区            ← 唯一 flex:1；必须 overflow:hidden
                              内容在区域内**垂直居中**（align-content:safe center）
                              —— 不是贴顶。空是「气口」，不是「没写完」
④ .quote    落点句（可选）     ← 永远贴在这个位置
⑤ .foot     页脚  中 = deck 全名 · 右 = 页码
```

### 容量（`u=28`）

| 页面上有什么 | 正文区 |
|---|---|
| 只有正文 | 420.8px |
| 正文 + 落点句（**1 行**） | 334.8px |
| 正文 + 落点句（**2 行**） | **274.4px** ← 多一行 −60.4px |
| 正文 + 导语 + 落点句 | **260.1px** ← 最紧 |

**落点句的目标长度是「一行说完」。两行不是更完整，是多花 60px。**

### 列宽（超了就折行，折行吃掉一整行容量）

| 列 | 宽 | 上限 |
|---|---|---|
| `.row .n` | 1.52u = 38px | 2 位数字 |
| `.row .k` | 5.6u = 140px | **4 个中文字** |
| `.row .k small` | 同上 | **14 个 ASCII** |
| `.loop .lnode` | 8u = 224px | **8 个中文字**（第 9 个字成孤字） |

### 零件

**完整清单（58 个，含示范页）：** `references/RULES.md` 里的 `BEGIN PARTS` 段，
或者跑 `"$SKILL/scripts/parts.py" --list`。

**最常用的：**

| 你要说的东西 | 用 |
|---|---|
| 并列条目，「第几」有含义 | `.rows`（四条带小注时加 `.tight`） |
| deck 的地图（**每份必有**） | `.toc` —— 条目 >5 先按主题归组 |
| deck 的收尾（**每份必有**） | `.end` ＋ `.qa` —— 标题 `Thanks` ＋ 正中 `Q & A` |
| 顺序不承载信息的短条目 | `.bullets` |
| 一句话一条的论点 | `.args`（五条时加 `.tight`） |
| 两栏对照 A 对 B | `.pair` |
| 3–4 个并列概念 | `.cards` |
| 有先后的几步 | `.flow` |
| **会回到起点** | `.loop` |
| 一页只有一句结论 | `.band` |
| 全页就是一页证据 | `.shot` + `.cap` |

---

## 5 · 出错了查这张表

**这些都是「错了但不报警」的。** 详细版 `references/FAILURES.md`。

| 你做了什么 | 会发生什么 | 屏幕上看得出吗 |
|---|---|---|
| 用 1px 分隔线 | 对方屏幕上线整体消失 | ✗ |
| 用近白底色（`#fcfcfd` 一档） | 几种底色塌成同一个灰 | ✗ |
| 用 `box-shadow` / `linear-gradient` | PDF 里出软掩码 | ✗ |
| 用系统字体（可变字体） | PDF 里几百个 Type 3，体积 ×6 | ✗ |
| 内联后改了文字没重跑 | 新字静默掉回系统字体 | ✗ |
| `.body` 没写 `overflow:hidden` | 内容静默压住页脚 | ✗ |
| 逐页切换深浅 | 整本像两套 deck 拼的 | **✗ 单页永远看不出** |
| 删 UI 时正则吃掉半个标签 | **整个 JS 死掉**（不居中 / 页码空） | ✗ 像「样式没加载」 |
| 忘了改外壳里的 `<title>` | 交付物顶着模板的名字 | ✗ |
| 弹性行里的元素被 `width:100%` 的兄弟挤成 0 宽 | **在 DOM 里但渲染不出来** | ✗ |
| 只按字宽盒子居中（`Q & A` / 页脚） | 两段空隙不等宽（短空隙时 6%） | **✗ 单页永远看不出** |
| 写填充语 / 夸张词（「值得注意的是」「颠覆性」） | 占位置不装信息，挤掉本当是证据的字 | ✓ 看得出 | ✓ 现在判失败 |
| 内容少 → 加灰字注解把余量警告垫平 | **check 绿了、页面更差**（观众看到一句废话） | ✓ 看得出，但你会以为修好了 | ✓ 余量按「内容高」算，垫字不再减余量 |
| 给共享的 `.body` 加 `display:flex`（想做居中） | 内联 `display:flex` 的横排页被翻成竖排 → 溢出 | ✓ | ✓ 溢出报错 |
| 溢出了就缩字号 | 破坏整套体系的前提 | ✓ 但**你会觉得是在帮忙** |

**`check.py` 抓得到一半，另一半只有两件事能抓：渲图看一眼，或者读 `references/FAILURES.md`。**

---

## 6 · 冻结线

| | |
|---|---|
| **令牌冻结** | `--u`、颜色、字体栈、间距、字号阶梯 —— **不许改** |
| **页面自由** | 用哪个原型、几页、文字怎么写 —— **随你** |
| **容量是硬墙** | 超了就删字 |
| **结构可加** | 需要新零件就往 CSS 里加，**但要跑 `parts.py` 给它补一页示范** |

> **一句话：页面结构你可以加，令牌值不要动。**

---

## 7 · 目录

**以下路径都相对于 `$SKILL`**（`SKILL.md` 所在目录）。

```
references/RULES.md      完整规范 + 推导 + 实测数据（唯一真相源）
references/BUILD.md      怎么跑（手动五步 + 四个坑）
references/FAILURES.md   ★ 静默失败清单 F1–F15 + 被证伪的假设
scripts/                 build.sh · check.py · build_font.py · inkcenter.py ·
                         newdeck.py · setpages.py · parts.py
assets/template.html     12 页零件库
assets/demo.html         用模板做的成品，9 页
assets/tests/            反向用例（`check.py --self-test` 用）
```

**改动先改 `references/RULES.md`**（唯一真相源），再考虑要不要同步到这一页。
